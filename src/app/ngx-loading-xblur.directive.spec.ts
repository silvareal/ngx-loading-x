import { NgxLoadingXBlurDirective } from './ngx-loading-xblur.directive';

describe('NgxLoadingXBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxLoadingXBlurDirective();
    expect(directive).toBeTruthy();
  });
});
